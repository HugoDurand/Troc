# Generated by Okty.io
version: '3'
services:
    php-fpm:
        build: docker/php/
        ports:
          - 9000:9000
        volumes:
            - './:/usr/share/nginx/html'
    nginx:
        build: docker/nginx/
        ports:
            - '8080:80'
        links:
            - php-fpm
        volumes:
            - './:/usr/share/nginx/html'